<%= form_for(@test_scaff) do |f| %>
  <% if @test_scaff.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@test_scaff.errors.count, "error") %> prohibited this test_scaff from being saved:</h2>

      <ul>
      <% @test_scaff.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
